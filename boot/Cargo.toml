[package]
name = "atahos_boot"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
log = "0.4"
uefi = { version = "0.36", features = ["panic_handler", "logger"] }
